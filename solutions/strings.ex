defmodule StringProblems do
  # Check if a string is a palindrome (ignoring case and non-alphanumeric characters)
  @spec palindrome?(String.t()) :: boolean
  def palindrome?(_str) do
    # Implement palindrome check
  end
end
