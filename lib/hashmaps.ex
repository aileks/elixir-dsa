defmodule MapProblems do
  # Count frequency of words in a list, return map with counts
  @spec word_frequency(list(String.t())) :: map
  def word_frequency(_words) do
    # Implement word frequency counter
  end
end
